# Defines which nodes are used in the system and how to connect to them.
# This element should only be referenced during the constructor's initial
# setup.
# All names must be unique.
# No other nodes are known.
# These names will be used for src and dest addresses for the Message
# The local node’s name is included in this list
configuration :
 - name : alice
   ip : 128.237.199.44
   port : 20000 # This is the incoming port on which MP will listen
                # May be changed for further communication as usual
 - name : bob
   ip : 128.237.199.44
   port : 20001
 - name : charlie
   ip : 128.237.165.94
   port : 20002
 - name : david
   ip : 128.237.165.94
   port : 20003
 - name : logger
   ip : 128.237.199.44
   port : 20004
   

sendRules :
 - action : drop # Ack message number 4 from bob to alice will not be sent
   src : bob
   dest : alice
   kind : Ack
   seqNum : 4
 - action : delay # Every Lookup message in the system will be delayed
   kind : Lookup
 #- action : duplicate

receiveRules :
 - action : duplicate # 3rd message from Charlie that anyone
   src : charlie # receives will be duplicated
   seqNum : 3
 #- action : drop
  # duplicate : true
   

# # YAML
# - name: PyYAML
#   status: 4
#   license: MIT
#   language: Python
# - name: PySyck
#   status: 5
#   license: BSD
#   language: Python
# # Java
# [{'status': 4, 'language': 'Python', 'name': 'PyYAML', 'license': 'MIT'},
# {'status': 5, 'license': 'BSD', 'name': 'PySyck', 'language': 'Python'}]


# {
#     configuration=[
#         {name=alice, ip=192.168.1.52, port=12344},
#         {name=bob, ip=192.168.1.112, port=14255},
#         {name=charlie, ip=128.2.130.19, port=12998},
#         {name=daphnie, ip=192.168.1.87, port=1987}
#         ],
#     sendRules=[
#         {action=drop, src=bob, dest=alice, kind=Ack, seqNum=4},
#         {action=delay, kind=Lookup}],
#     receiveRules=[
#         {action=duplicate, src=charlie, seqNum=3}
#         ]
# }
